/*
1. this is a challenges in which we are going to create a simple medicines 
records to capture the medicine name, quantity, unit price, isExpired, isPrescription, 
selected batch

2. we shall also print the data types of all these 
variables
*/


// first we are assigning the variable names

const medicineName = "Valsatan";

let quantity = 500;

const unitPrice = 3500;

let isExpired;

let isPrescriptionOnly = null;

let selectedBatch = true;

stockValue = unitPrice * quantity

// now print the values of the variables and their 
// data types as rendered by JavaScript

console.log(
    "Medicine: " + (medicineName)
);

console.log(
    "Quantity: " + (quantity)
);

console.log(
    "Unit Price: " + (unitPrice)
);

console.log(
    "Is Expired: " + (isExpired)
);


// stock valuation
console.log(
    "Stock Valuation: " + (stockValue)
)



