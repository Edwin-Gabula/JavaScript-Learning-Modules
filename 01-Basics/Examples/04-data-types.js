/*
exploring the various data typpes
used in JS
*/

const medicine = "Paracetamol";   // string data type

const stock = 250;   // number data type

const isAvailable = true;   // boolean data type

let selectedBatch;    // undefined data type

const batchNumber = null;    // null data type

// printhing the output to showcase the various data types used in JS

console.log(
    "Medicine: " + (typeof medicine)
);

console.log(
    "Stock: " + (
        typeof stock
    )
);

console.log(
    "Is Available: " + (
        typeof isAvailable
    )
);

console.log(
    "Selected Batch: " + (
        typeof selectedBatch
    )
);

console.log(
    "Batch Number: " + (
        typeof batchNumber
    )
)