/*
creating variables to store the following
info and displaying the data types therof
*/

const medicineName = "Amlodipine";

const unitPrice = 2500;

let stockQuantity = 150;

let isAvailable = false;

let selectedCustomer = true;

// print the above data and its data types
console.log(
    "Medicine: " + (medicineName),
    "Type: " + (typeof medicineName)
)

console.log(
    "Unit Price: " + (unitPrice),
    "Type: " + (
        typeof unitPrice
    )
)

console.log(
    "Available: " + (typeof isAvailable),
    "Type: " + (
        typeof isAvailable
    )
)